# Core scientific computing
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.11,<2
scikit-learn>=1.3,<2

# Topological Data Analysis
ripser>=0.6.4,<1
persim>=0.3.5,<1

# Plotting
matplotlib>=3.7,<4

# Networks (for Mapper algorithm in Strategy 2)
networkx>=3.1,<4

# HTTP / data fetching
requests>=2.31,<3

# PDF generation (used by scripts/build_pdf.py)
reportlab>=4.0,<5
pypdf>=4.0,<6
markdown>=3.5,<4

# Stronger ML baselines used in v11 (LOAO).
# xgboost and lightgbm require an OpenMP runtime — on macOS install via
# `brew install libomp` before pip install.
xgboost>=2.0,<4
lightgbm>=4.0,<5
catboost>=1.2,<2
statsmodels>=0.14,<1

# Parquet IO for the v11 pooled-data cache.
pyarrow>=14,<22
